```json
{
 "cells": [
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "# Step 1: Image Analysis and Parameter Extraction\n",
    "\n",
    "This notebook processes dosimetric screenshots and extracts key parameters using OCR.\n",
    "\n",
    "## Parameters to Extract:\n",
    "- Fluoro time\n",
    "- Total fluoro Kair\n",
    "- Dose entrée peau max\n",
    "- Exposit NI\n",
    "- Total PDS\n",
    "- Total dose\n",
    "- Fréquence acquisition (F/S)"
   ]
  },
  {
   "cell_type":  "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Import libraries\n",
    "import sys\n",
    "sys.path.append('. .')\n",
    "\n",
    "from src.image_processor import ImageProcessor, batch_preprocess_images\n",
    "from src.parameter_extractor import ParameterExtractor\n",
    "import pandas as pd\n",
    "import matplotlib.pyplot as plt\n",
    "from pathlib import Path\n",
    "\n",
    "print(\"✓ Libraries imported successfully\")"
   ]
  },
  {
   "cell_type":  "markdown",
   "metadata":  {},
   "source": [
    "## 1.1 Preprocess Images"
   ]
  },
  {
   "cell_type":  "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Define paths\n",
    "raw_images_path = '../data/raw/'\n",
    "processed_images_path = '../data/processed/preprocessed_images/'\n",
    "\n",
    "# Preprocess all images\n",
    "batch_preprocess_images(raw_images_path, processed_images_path)"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source":  [
    "## 1.2 Test OCR on Single Image"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Test on a single image first\n",
    "test_image = list(Path(raw_images_path).glob('*. png'))[0]  # Get first image\n",
    "print(f\"Testing on: {test_image.name}\")\n",
    "\n",
    "# Initialize extractor\n",
    "extractor = ParameterExtractor(ocr_engine='tesseract')  # or 'easyocr'\n",
    "\n",
    "# Extract parameters\n",
    "params = extractor. extract_from_image(str(test_image))\n",
    "\n",
    "# Display results\n",
    "print(\"\\nExtracted Parameters:\")\n",
    "for key, value in params.items():\n",
    "    print(f\"  {key}: {value}\")"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 1.3 Batch Extract All Images"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Extract from all images\n",
    "output_csv = '../data/processed/extracted_parameters.csv'\n",
    "\n",
    "df = extractor.batch_extract(raw_images_path, output_csv)\n",
    "\n",
    "# Display results\n",
    "print(\"\\nExtraction complete!\")\n",
    "print(f\"Total interventions processed: {len(df)}\")\n",
    "print(\"\\nFirst few rows:\")\n",
    "df.head()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 1.4 Data Quality Check"
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Check for missing values\n",
    "print(\"Missing values per parameter:\")\n",
    "print(df.isnull().sum())\n",
    "\n",
    "# Check data types\n",
    "print(\"\\nData types:\")\n",
    "print(df.dtypes)\n",
    "\n",
    "# Basic statistics\n",
    "print(\"\\nBasic statistics:\")\n",
    "df.describe()"
   ]
  },
  {
   "cell_type": "markdown",
   "metadata": {},
   "source": [
    "## 1.5 Manual Verification\n",
    "\n",
    "**Action Required**: Manually verify a subset of extractions to ensure accuracy."
   ]
  },
  {
   "cell_type": "code",
   "execution_count": null,
   "metadata": {},
   "outputs": [],
   "source": [
    "# Display random samples for manual verification\n",
    "sample_size = 5\n",
    "samples = df.sample(n=sample_size)\n",
    "\n",
    "print(f\"Random sample of {sample_size} extractions for verification:\")\n",
    "samples"
   ]
  },
  {
   "cell_type":  "markdown",
   "metadata":  {},
   "source": [
    "## Next Steps\n",
    "\n",
    "1. ✓ Images preprocessed and parameters extracted\n",
    "2. → Proceed to `02_statistical_analysis.ipynb` to calculate DRLs\n",
    "3. → Compare with manual extraction in `03_comparison_validation.ipynb`"
   ]
  }
 ],
 "metadata": {
  "kernelspec": {
   "display_name": "Python 3",
   "language":  "python",
   "name":  "python3"
  },
  "language_info": {
   "name": "python",
   "version": "3.8.0"
  }
 },
 "nbformat": 4,
 "nbformat_minor":  4
}
```